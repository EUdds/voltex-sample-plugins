{
    "name": "voltex-sample-plugins",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "node build.mjs",
        "build:watch": "node build.mjs --watch"
    },
    "devDependencies": {
        "@voltex-viewer/plugin-api": "^0.3.0",
        "terser": "^5.44.0",
        "typescript": "^5.0.0",
        "vite": "^5.0.0"
    }
}
